# sdk

Internal SDK clients for cross-service contracts.

## Initial Scope

- typed models generated from `packages/schemas`
- HTTP clients for service endpoints
- policy-gateway routing helpers

## Rule

SDK must remain a thin contract layer; no business logic.
